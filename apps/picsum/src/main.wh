var seed = 1

fun refresh() {
  seed = seed + 1
}

<Column padding={16} spacing={8}>
  <Button text="New Image" onClick={refresh} />
  <Image src={"https://picsum.photos/seed/${seed}/300/200"} alt="Random photo" width="100%" height="300dp" />
</Column>
