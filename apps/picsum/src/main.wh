var seed = 1

fun refresh() {
  seed = seed + 1
}

<Column padding={16} spacing={8}>
  <Image src={"https://picsum.photos/seed/${seed}/300/200"} alt="Random photo"/>
  <Button text="New Image" onClick={refresh} />
</Column>
