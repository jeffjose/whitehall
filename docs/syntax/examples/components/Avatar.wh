// Shared Avatar component
// Location: src/shared/Avatar.wh
// Import as: import $shared.Avatar

  @prop val url: String
  @prop val size: Int = 48
  @prop val onClick: (() -> Unit)? = null

<AsyncImage
  url={url}
  width={size}
  height={size}
  cornerRadius={size / 2}
  onClick={onClick}
  contentDescription="Avatar"
/>
