[project]
name = "Microblog"
version = "0.1.0"

[android]
min_sdk = 24
target_sdk = 34
package = "com.example.microblog"

[build]
output_dir = "build"

# Note: Import aliases ($app, $components, etc.) are automatically resolved by the transpiler
# $components → com.example.microblog.components
# $models → com.example.microblog.models
# $lib → com.example.microblog.lib
# $app → com.example.microblog

# Note: Dependencies are currently hardcoded in the scaffold generator
# Material3, Compose, Navigation, and Coil are included by default
