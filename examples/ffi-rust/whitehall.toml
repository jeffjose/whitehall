[project]
name = "ffi-rust-example"
version = "0.1.0"

[android]
package = "com.example.ffirust"
min_sdk = 24
target_sdk = 34

[ffi]
enabled = true
ffi_only = true  # Required: Cargo/CMake native compilation not yet implemented (Phase 1.6)

[ffi.rust]
profile = "release"
