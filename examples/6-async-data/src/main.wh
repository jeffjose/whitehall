/// [app]
/// name = "Async Data Loading"
/// package = "com.example.asyncdata"

// Example 6: Async Data Loading
// Tests: Suspend functions, async operations, loading states, error handling
// Now structured as a multi-file project with stores and components

import $stores.PostStore
import $components.PostListView

val store = PostStore()

<PostListView store={store} />
